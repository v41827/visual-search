# Ignore everything by default
*

# Allow directories so we can whitelist specific folders/files
!*/

# Keep the Skeleton_Python directory and all its contents
!Skeleton_Python/
!Skeleton_Python/**

# Keep only the refresh_report.sh script; ignore other shell scripts
!refresh_report.sh

# Keep top-level README
!README.md

# Keep CNN training scripts at repo root
!cnn_train.py
!cnn_scratch_train.py

# Keep tools folder (batch helpers)
!tools/
!tools/**

# Keep the results folder itself, but ignore its contents by default
!results/
!results/.gitkeep

# Ignore macOS Finder files everywhere
.DS_Store
**/.DS_Store

# Keep this .gitignore
!.gitignore

# Note: Git does not track empty directories. Add placeholder files to ensure they are committed:
# touch results/.gitkeep results/eval/.gitkeep results/search/.gitkeep
